{
  "name": "protractor-cucumber-framework-uncaught-exceptions",
  "version": "1.0.0",
  "scripts": {
    "webdriver": "webdriver-manager update --standalone --versions.standalone=2.53.1 && webdriver-manager start --versions.standalone=2.53.1",
    "start": "node testapp/server",
    "test": "protractor"
  },
  "devDependencies": {
    "cucumber": "1.3.2",
    "express": "4.15.2",
    "protractor": "4.0.10",
    "protractor-cucumber-framework": "3.0.0"
  }
}
